# ══════════════════════════════════════════════════════════════════════════════
# AAA Game Loading Screen Generator — Python dependencies
# ══════════════════════════════════════════════════════════════════════════════

streamlit>=1.35.0
Pillow>=10.3.0
numpy>=1.26.0
requests>=2.31.0

# ── System dependency (NOT a pip package) ─────────────────────────────────────
# FFmpeg must be installed at the OS level:
#
#   Streamlit Cloud → create a file called  packages.txt  in your repo root
#                     containing a single line:  ffmpeg
#
#   Linux (local)   → sudo apt-get install -y ffmpeg
#   macOS (local)   → brew install ffmpeg
#   Windows (local) → choco install ffmpeg
#                     or download from https://ffmpeg.org/download.html
#
# The app gracefully shows an install hint if ffmpeg is not found.
